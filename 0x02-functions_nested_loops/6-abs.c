#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _abs - does some tings
 * @n: The value to print
 *
 * Return: On success 1.
 * On fail, 0 is returned.
 * On something, -1
 */
int _abs(int n)
{
if (n < 0)
{n *= -1; }}
