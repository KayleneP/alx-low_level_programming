#include <stdio.h>
#include <string.h>
/**
 * _islower - does some tings
 * @c: The value to print
 *
 * Return: On success 1.
 * On fail, 0 is returned.
 */
int _islower(int c)
{
if (c > 96)
{return (1); }
else
{return (0); }
}
