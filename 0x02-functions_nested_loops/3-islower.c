#include <stdio.h>
#include <string.h>
/**
 * _islower - Entry point
 * Description: _islower does some tings
 * Return: void (Success)
 */
int _islower(int c)
{if (c > 96)
{return (1); }
else
{return (0); }
}
