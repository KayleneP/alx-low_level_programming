#include <stdio.h>
#include <string.h>
/**
 * print_alphabet - Entry point
 * Description: print_alphabet does some tings
 * Return: void (Success)
 */
void print_alphabet(void)
{int x;
for (x = 97; x < 123; x++)
{putchar(x); }
putchar(10); }
